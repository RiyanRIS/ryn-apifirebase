class User {
    constructor(id, nama, nama_depan, nohp, alamat ) {
            this.id = id;
            this.nama = nama;
            this.nama_depan = nama_depan;
            this.nohp = nohp;
            this.alamat = alamat;
    }
}

module.exports = User;